{
    "name": "pca-admin-backend",
    "version": "1.0.0",
    "description": "PCA Admin API Backend",
    "private": true,
    "scripts": {
        "build": "echo 'No frontend build required for API backend'",
        "start": "php artisan serve --host=0.0.0.0 --port=${PORT:-8080}",
        "dev": "php artisan serve --host=0.0.0.0 --port=8002"
    },
    "devDependencies": {},
    "dependencies": {}
}