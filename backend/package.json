{
    "name": "pca-backend",
    "version": "1.0.0",
    "description": "PCA Student API Backend",
    "private": true,
    "scripts": {
        "build": "echo 'No frontend build required for API backend'",
        "start": "php artisan serve --host=0.0.0.0 --port=${PORT:-8080}",
        "dev": "php artisan serve --host=0.0.0.0 --port=8000"
    },
    "devDependencies": {},
    "dependencies": {}
}